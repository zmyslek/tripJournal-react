function Gallery() {
    return (
        <section className="page-placeholder" aria-label="Gallery page">
            <h2 className="page-placeholder__title">Gallery</h2>
            <p className="page-placeholder__text">This page is intentionally empty for now.</p>
        </section>
    );
}

export default Gallery;
